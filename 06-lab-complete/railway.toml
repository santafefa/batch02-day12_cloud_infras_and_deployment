# railway.toml
# Docs: https://docs.railway.app/reference/railway-toml

[build]
builder = "NIXPACKS" # Use Nixpacks for automatic build detection

[deploy]
startCommand = "sh -c 'uvicorn app.main:app --host 0.0.0.0 --port $PORT'"
healthcheckPath = "/health"